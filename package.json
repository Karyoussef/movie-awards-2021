{
  "name": "movies-awards-2021",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@reduxjs/toolkit": "^1.9.7",
    "concurrently": "^8.2.2",
    "json-server": "^0.17.4",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-loader-spinner": "^5.4.5",
    "react-redux": "^8.1.3",
    "react-scripts": "5.0.1",
    "react-spring": "^9.7.3",
    "web-vitals": "^2.1.4"
  },
  "scripts": {
    "start": "concurrently -k --success first \"json-server --watch db/db.json --port 3001\" \"react-scripts start\"",
    "build": "react-scripts build"
  },
  "eslintConfig": {
    "extends": [
      "react-app"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
